output: dist/
releases:
  - name: dev
    jobs:
      - name: release-linux
        package:
          platform: linux
          targets: [appimage, deb, rpm]
          build_args:
            dart-define:
              APP_ENV: dev
